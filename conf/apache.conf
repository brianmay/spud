<Directory /var/www>
    <IfModule mod_rewrite.c>
      Options +symlinksifownermatch
      RewriteEngine On
      RewriteBase /
      RewriteCond %{request_filename} !-f
      RewriteCond %{request_filename} !-d
      RewriteRule ^(spud(/.*)?)$ /spud.fcgi/$1 [l,qsa]
    </IfModule>
</Directory>

Alias /media /usr/share/spud/media
Alias /admin_media /usr/share/pyshared/django/contrib/admin/media

FastCGIExternalServer /var/www/spud.fcgi -socket /var/run/spud/photos.socket
